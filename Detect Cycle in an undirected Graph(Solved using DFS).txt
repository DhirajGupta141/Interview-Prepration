Given an undirected graph with V vertices and E edges, check whether it contains any cycle or not. 

Example 1:

Input:   

Output: 1
Explanation: 1->2->3->4->1 is a cycle.
Example 2:

Input:
 

Output: 0
Explanation: No cycle in the graph.

_____________________________________________________________SOLUUTION________________________

class Solution
{
    
    boolean dfsHelper(int curr, int parent,boolean vis[]
                                ,ArrayList<ArrayList<Integer>> adj)
    {
        vis[curr]=true;
        //exploring adjecent nodes
        for(int ad:adj.get(curr))
        {
            if(vis[ad]==false)
            {
                if(dfsHelper(ad,curr,vis,adj))//going depth if some one returns true
                    return true;
            }
            else
            {
                if(parent!=curr)//if current element is not parent and it is already visisted means cycle is there for sure 
                {
                    return true;
                }
            }
        }
        
        return false;
    }
    
    public boolean isCycle(int V, ArrayList<ArrayList<Integer>> adj)
    {
        boolean vis[]=new boolean[V];
        
        for(int i=0;i<V;i++)
        {
            if(vis[i]==false)
            {
                //why if block --> because we are hopping if any of the component returns true
                if(dfsHelper(i,-1,vis,adj)) // -1 is parent or prev element
                {
                    return true;
                }
            }
        }
        return false;
        
    }
}
